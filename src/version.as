// History
//
//	Release			Index
//	3.0-alpha4		1

public static var RELEASE_IDX: Number = 1;

public static var VERSION_MAJOR: Number = 3;
public static var VERSION_MINOR: Number = 0;

public static var VERSION_ALPHA: Number = 0;
public static var VERSION_STRING: String = (VERSION_MAJOR + "." + VERSION_MINOR + "-alpha" + VERSION_ALPHA);

//public static var VERSION_BETA: Number = 0;
//public static var VERSION_STRING: String = (VERSION_MAJOR + "." + VERSION_MINOR + "-beta" + VERSION_BETA);

//public static var VERSION_STRING: String = (VERSION_MAJOR + "." + VERSION_MINOR);